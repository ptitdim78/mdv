{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="element-title">
                <h1 id="titreSection">Nos créations</h1>
            </div>
        </div>
        <div class="container">
            <div class="row">
                <h3 style="text-align: justify">
                    Vers 1454, l'allemand Johannes Gutenberg inventa les caractères mobiles, aujourd’hui, nous vous
                    proposons des actions de communication et surtout d’impression vers les différents types de supports
                    soumis en permanence à notre veille technologique :
                </h3>
                <hr>
                <ul style="text-align: justify; font-size: 20px;">
                    <li style="color: black!important;" class="listCrea"> - La croissance, les évolutions techniques et réglementaires...</li>
                    <li style="color: black!important;" class="listCrea"> - Vos attentes : types de besoins, budget, fréquence de renouvellement...</li>
                    <li style="color: black!important;" class="listCrea"> - Nos actions pour vous servir que nous vous invitons à découvrir sur notre site et nos pages sur les réseaux sociaux…</li>
                </ul>
            </div>
        </div>
        <div class="row">
            <div class="element-title">
                <h1 id="titreSection">Signalétique</h1>
            </div>
        </div>
        <div class="row productContainer">
            {% for creation in gallery %}
                {% if creation.domaine == 'Signaletique' %}
                    <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                        <div class="thumbnail tailleThumbnail">
                            {% if creation.image !=null %}
                                <img class="imgThumbnailGallery" src="{{ vich_uploader_asset(creation, 'imageFile') }}"
                                     alt="gallerie signalétique">
                            {% else %}
                                <img class="imgThumbnailGallery" src="{{ asset('images/affiche.png') }}"
                                     alt="gallerie signalétique">
                            {% endif %}
                            <div class="caption captionGallery" style="padding: 0!important;">
                                <h3 class="titleGallery">{{ creation.title }}</h3>
                            </div>
                            {% if creation.description != null %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">{{ creation.description }}</h5>
                                </div>
                            {% else %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">Création réalisée par Lemoutondesvilles.
                                        "Vous aider à créer une image qui vous ressemble.." #SylvieMdesigner</h5>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
        <div class="row">
            <div class="element-title">
                <h1 id="titreSection">Bâche</h1>
            </div>
        </div>
        <div class="row productContainer">
            {% for creation in gallery %}
                {% if creation.domaine == 'Bache' %}
                    <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                        <div class="thumbnail tailleThumbnail">
                            {% if creation.image !=null %}
                                <img class="imgThumbnailGallery" src="{{ vich_uploader_asset(creation, 'imageFile') }}"
                                     alt="gallerie bâche">
                            {% else %}
                                <img class="imgThumbnailGallery" src="{{ asset('images/affiche.png') }}"
                                     alt="gallerie bâche">
                            {% endif %}
                            <div class="caption captionGallery" style="padding: 0!important;">
                                <h3 class="titleGallery">{{ creation.title }}</h3>
                            </div>
                            {% if creation.description != null %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">{{ creation.description }}</h5>
                                </div>
                            {% else %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">Création réalisée par Lemoutondesvilles.
                                        "Vous aider à créer une image qui vous ressemble.." #SylvieMdesigner</h5>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
        <div class="row">
            <div class="element-title">
                <h1 id="titreSection">Marquage</h1>
            </div>
        </div>
        <div class="row productContainer">
            {% for creation in gallery %}
                {% if creation.domaine == 'Marquage' %}
                    <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                        <div class="thumbnail tailleThumbnail">
                            {% if creation.image !=null %}
                                <img class="imgThumbnailGallery" src="{{ vich_uploader_asset(creation, 'imageFile') }}"
                                     alt="gallerie marquage">
                            {% else %}
                                <img class="imgThumbnailGallery" src="{{ asset('images/affiche.png') }}"
                                     alt="gallerie marquage">
                            {% endif %}
                            <div class="caption captionGallery" style="padding: 0!important;">
                                <h3 class="titleGallery">{{ creation.title }}</h3>
                            </div>
                            {% if creation.description != null %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">{{ creation.description }}</h5>
                                </div>
                            {% else %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">Création réalisée par Lemoutondesvilles.
                                        "Vous aider à créer une image qui vous ressemble.." #SylvieMdesigner</h5>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
        <div class="row">
            <div class="element-title">
                <h1 id="titreSection">Logo</h1>
            </div>
        </div>
        <div class="row productContainer">
            {% for creation in gallery %}
                {% if creation.domaine == 'Logo' %}
                    <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                        <div class="thumbnail tailleThumbnail">
                            {% if creation.image !=null %}
                                <img class="imgThumbnailGallery" src="{{ vich_uploader_asset(creation, 'imageFile') }}"
                                     alt="gallerie logo">
                            {% else %}
                                <img class="imgThumbnailGallery" src="{{ asset('images/affiche.png') }}"
                                     alt="gallerie logo">
                            {% endif %}
                            <div class="caption captionGallery" style="padding: 0!important;">
                                <h3 class="titleGallery">{{ creation.title }}</h3>
                            </div>
                            {% if creation.description != null %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">{{ creation.description }}</h5>
                                </div>
                            {% else %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">Création réalisée par Lemoutondesvilles.
                                        "Vous aider à créer une image qui vous ressemble.." #SylvieMdesigner</h5>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
        <div class="row">
            <div class="element-title">
                <h1 id="titreSection">Immobilier</h1>
            </div>
        </div>
        <div class="row productContainer">
            {% for creation in gallery %}
                {% if creation.domaine == 'Immobilier' %}
                    <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                        <div class="thumbnail tailleThumbnail">
                            {% if creation.image !=null %}
                                <img class="imgThumbnailGallery" src="{{ vich_uploader_asset(creation, 'imageFile') }}"
                                     alt="gallerie immobilier">
                            {% else %}
                                <img class="imgThumbnailGallery" src="{{ asset('images/affiche.png') }}"
                                     alt="gallerie immobilier">
                            {% endif %}
                            <div class="caption captionGallery" style="padding: 0!important;">
                                <h3 class="titleGallery">{{ creation.title }}</h3>
                            </div>
                            {% if creation.description != null %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">{{ creation.description }}</h5>
                                </div>
                            {% else %}
                                <div class="descriptionGallery">
                                    <h5 style="font-size: large">Création réalisée par Lemoutondesvilles.
                                        "Vous aider à créer une image qui vous ressemble.." #SylvieMdesigner</h5>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>

{% endblock %}